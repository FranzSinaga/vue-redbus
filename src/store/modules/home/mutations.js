const setForm = (state, form) => {
    state.form = form;
}

const setDestination = (state, destination) => {
    state.destination = destination;
}

export default {
    setForm,
    setDestination
}